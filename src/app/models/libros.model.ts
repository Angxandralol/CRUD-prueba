export class LibroModelo{
    //variable + ! = esta variable no puede ser null o undefined
    n_referencia!: number;
    titulo!: string;
    descripcion?: string;
    //Variable + ? = esta variable es de tipo opcional. Se mostrará "undefined" si no se usa la variable
    
}